name: Lint and formatter
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Run black
    run: 
      black --check .
    - name: Run flake8
    run: flake8